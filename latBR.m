%latBR
%Load-and-trigger, Black Rock Files

function [vargout] = latBR(vargin)
test = vargin;
vargout = test;

%% 1) Load Session Params

%% 2) Load NEV, NS2, NS6

%% 3) order pins for Plexon or NN

%% 4) Trigger

    % 4.a) Trig to EventCodes
    
    % 4.b) Trig to PhotoDiode if exists
    
%% 5) Get important trials

    % 5.a) Save variable with all trials for overview
    
    % 5.b) Save variables with BRFS-specific condition onsets








%% Output is triggeredNS2 and triggeredNS6
end